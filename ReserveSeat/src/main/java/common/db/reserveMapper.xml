<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.ac.kopo.dao.ReserveDAO">
	<insert id="insertReserve" parameterType="map">
		INSERT INTO rs_regist (user_id, class_no, reg_time, seat_row, seat_col)
		VALUES (#{userId}, #{classNo}, #{regTime}, #{seatRow}, #{seatCol})
	</insert>
</mapper>